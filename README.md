# DevBoost MCP Server 🚀

DevBoost — это MCP-сервер, созданный для решения частых болей локальной разработки:
1. Забытые и "залипшие" порты (`EADDRINUSE`).
2. Рассинхрон локальных версий инструментов (Node, Python).
3. Медленные SQL-запросы в БД и сложность чтения `EXPLAIN ANALYZE`.

## Инструменты (Tools)

- `check_versions(tools: string[])`: Проверка версий установленного локально ПО.
- `kill_port_hog(port: number)`: Находит и безопасно завершает процесс, занимающий порт (только текущего пользователя).
- `explain_query(query: string)`: Выполняет `EXPLAIN ANALYZE` для SQL-запроса в PostgreSQL и возвращает план.
- `suggest_index(tables: string[])`: Анализирует таблицы и показывает существующие индексы, чтобы помочь AI предложить оптимизацию.

## Требования
- Python 3.10+
- Docker и Docker Compose (для демо-БД)

## Как подключить к ИИ (Claude Desktop / Cursor)

MCP-сервер работает как локальный плагин для ИИ-ассистентов. Для того чтобы ИИ научился понимать узкие места в вашей БД или решать системные проблемы, сервер добавляется в его конфиг:

### Для Claude Desktop

1. Откройте файл конфигурации:
   - **MacOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

2. Добавьте наш сервер, заменив пути на абсолютные (до вашей папки с проектом):
```json
{
  "mcpServers": {
    "devboost": {
      "command": "/АБСОЛЮТНЫЙ_ПУТЬ_ДО_ВЕНВ_ЕСЛИ_НУЖНО/bin/python",
      "args": ["/АБСОЛЮТНЫЙ_ПУТЬ_ДО_ПРОЕКТА/run_stdio.py"],
      "env": {
        "DATABASE_URL": "postgresql://testuser:testpass@localhost:5432/testdb"
      }
    }
  }
}
```
3. Перезапустите Claude Desktop и скажи: *"Проверь версии моего локального окружения через devboost"*.

### Для Cursor IDE

1. Зайдите в **Settings** -> **Features** -> **MCP**.
2. Нажмите **+ Add New MCP Server**.
3. Выберите Type: `command`.
4. Name: `devboost`.
5. Command: Укажите полный путь: `/ПОЛНЫЙ_ПУТЬ/.venv/bin/python /ПОЛНЫЙ_ПУТЬ/run_stdio.py`

## Тестирование и Демо-база
Для запуска тестовой БД с 100 000 записей и проверки через Inspector браузера, прочитайте инструкцию **[DEMO.md](DEMO.md)**.

## Участие в хакатоне
Сервер разработан с использованием AI (Agentic Engineering):
- Проведено пред-исследование болей (PLANNING).
- Созданы артефакты (task.md, implementation_plan.md).
- Использована слоистая архитектура для соблюдения SOLID (Transport -> Services -> Core).
